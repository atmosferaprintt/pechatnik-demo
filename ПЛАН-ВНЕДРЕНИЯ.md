# План внедрения CRM «ПЕЧАТНИК» — боевой запуск

Составлен 2026-07-15 (вечер), старт — 2026-07-16. Демо утверждено Кристи, переводим на реальную инфраструктуру. Порядок повторяет проверенную схему PrintCRM (VPS Beget + Docker + self-hosted Supabase), но с включённым RLS.

## Этап 0 · Вводные (нужно от Рабии/Кристи ДО начала — блокеры)

- [ ] **VPS**: заказать новый (Beget, как у PrintCRM, ~4ГБ RAM под Supabase) — доступы root
- [ ] **Домен**: как называем? (например `pechatnik-crm.ru` / поддомен) — купить, DNS на VPS
- [ ] **Банк Кристи** для расчётного счёта — какой? (для API выписки в сверку)
- [ ] **Почты сотрудниц** для логинов: Кристи, Алена, Настя, Влада, Марьян, Людмила
- [ ] **Финальные справочники от Кристи**: категории доходов/расходов (за основу — её Excel), 4–5 карт-банков для переводов, подкатегории личных

## Этап 1 · Инфраструктура (~полдня при готовом VPS)

1. VPS: Docker + docker-compose, firewall, ssh-ключи
2. Self-hosted Supabase (по образцу `/opt/supabase` PrintCRM): Postgres, Auth, REST, Studio за basic auth
3. Caddy (или nginx) reverse-proxy + авто-SSL на домен
4. `scripts/deploy.sh` — rsync → docker build (node → nginx) → up, как в PrintCRM
5. **Сразу** бэкапы: cron `pg_dump` ежедневно + offsite (R2/B2) — урок PrintCRM, не откладывать в TODO

## Этап 2 · База данных (~полдня)

1. Переписать `supabase/migrations/001_init.sql` под текущую модель демо:
   - `tasks`: + `assignee text`, `done bool`, `parts jsonb`, `contractor_id`; `task_log` отдельной таблицей (who/action/time)
   - новые таблицы: `contractors`, `contractor_tasks`, `deposits` + `deposit_uses`, `manual_debts` + `manual_debt_entries`, `supply_items`
   - `clients.prices jsonb` (индивидуальные цены)
   - `transactions.batch_id` (разбивка одной оплаты на статьи)
2. **RLS-политики** (002_rls.sql): сотрудник — вставка операций + чтение ТОЛЬКО своих за сегодня; приватные категории (`expense_work/personal`), `day_closures`, чужие transactions — только owner. Задачи/клиенты/контрагенты/поставка — чтение-запись всем авторизованным
3. Seed (003_seed.sql): категории из Excel Кристи, банки-карты, профили 6 пользователей

## Этап 3 · Перевод кода с демо-state на Supabase (~1–2 дня)

Порядок секций (от критичного):
1. `App.jsx`: реальный auth (email/password), `loadAll` из таблиц, realtime-подписки (`postgres_changes`) на tasks/transactions
2. **Финансы**: запись операций (batch), лента дня, сверка, day_closures «Закрыть день»
3. **Задачи**: канбан + task_log, завершение, долги (+ manual_debts)
4. **Клиенты** (+prices), **Контрагенты** (+contractor_tasks), **Депозиты**, **Поставка**
5. **Настройки**: CRUD категорий, банков, пользователей (создание через Supabase Auth admin)
6. **Аналитика** и **Мой день**: реальные агрегаты по месяцу/дню
7. Демо-режим сохранить: без `.env.local` работает как сейчас (для показов)

## Этап 4 · Доступы и обучение (~полдня)

1. Завести 6 аккаунтов, раздать пароли
2. Памятки: короткая для девочек («как записать приход за 5 секунд», по образцу Памяток PrintCRM) + для Кристи (сверка дня, долги, аналитика)
3. Прогон с Кристи по видеосвязи

## Этап 5 · Перенос данных (~полдня, параллельно с 4)

1. Из Excel Кристи: категории (уже в seed), июльские операции — НЕ переносим (начинаем с чистого дня), но итоги июня — спросить, нужны ли для аналитики
2. Клиенты с индивидуальными ценами (спросить у Кристи список)
3. Текущие долги клиентов и действующие депозиты — завести руками при старте
4. Остаток кассы на утро первого дня — стартовая точка сверки

## Этап 6 · Банковская выписка (после запуска, отдельная итерация)

1. Узнать банк → API/выгрузка (посмотреть `CardStatementImport.jsx` PrintCRM что переиспользовать)
2. До интеграции: сверка по ручному вводу «пришло на счёт» — уже работает
3. Потом: автозагрузка в `bank_statements` + автоматчинг с транзакциями

## Этап 7 · Запуск и сопровождение

1. **Тестовая неделя**: девочки ведут CRM параллельно с тетрадью, вечером сверяем расхождения
2. Сбор правок от Кристи → итерации (как сейчас)
3. После стабильной недели — тетрадь на пенсию 🎉
4. Деплой-правила: изменения → `./scripts/deploy.sh` → commit + push (по правилам CLAUDE.md)

## Ориентир по срокам

| День | Что |
|---|---|
| 16.07 | Этап 0 (вводные от Рабии) + Этап 1 + Этап 2 |
| 17–18.07 | Этап 3 (перевод секций на Supabase) |
| 18.07 | Этапы 4–5 (аккаунты, памятки, стартовые данные) |
| 19.07 | Запуск тестовой недели с Кристи |
| ~26.07 | Разбор недели, правки, полный переход |

Блокер №1 на завтра: VPS и домен — без них начинаем с этапа 2 (миграции пишутся локально).
